# Testing

Run tests with:

```bash
cd backend
pip install pytest httpx
pytest ../tests/ -v
```

## Test Structure

| Directory | Tests |
|-----------|-------|
| `tests/test_health.py` | Health endpoint tests (working) |
| `tests/backend/` | API endpoint tests (placeholder) |
| `tests/ml/` | ML preprocessing tests (placeholder) |
